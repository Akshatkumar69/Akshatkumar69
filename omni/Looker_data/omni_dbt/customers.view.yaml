# Reference this view as omni_dbt__customers
label: Customers
schema_label: ""
#This description was pulled from dbt.
description: Customer dimension table from the workspace.

schema: omni_dbt
table_name: customers

dimensions:
  city: {}
  signup_date: {}

  customer_id:
    format: ID
    #This description was pulled from dbt.
    description: Unique identifier for a customer.
    primary_key: true

  customer_name:
    #This description was pulled from dbt.
    description: Full name of the customer.

  _entity_customer:
    # Entity is defined in the 'customers' dbt semantic model (models/semantic_models.yml)
    sql: ${omni_dbt__customers.customer_id}
    hidden: true

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: customers
  target_schema: default
  description: Customer dimension table from the workspace.
  config:
    materialized: view
  code: |-
    SELECT
        CUSTOMER_ID,
        CUSTOMER_NAME
    FROM workspace.default.CUSTOMERS
  referenced_by: [ fct_customer_orders ]
