# Relationship is defined in the 'orders' dbt semantic model (models/semantic_models.yml)
- join_from_view: workspace_default__orders
  join_to_view: workspace_default__customers
  join_type: always_left
  on_sql: ${workspace_default__orders._entity_customer} =
    ${workspace_default__customers._entity_customer}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__cdc_orders
  join_to_view: workspace_default__orders
  join_type: always_left
  on_sql: ${workspace_default__cdc_orders.order_id} =
    ${workspace_default__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__fact_orders
  join_to_view: workspace_default__orders
  join_type: always_left
  on_sql: ${workspace_default__fact_orders.order_id} =
    ${workspace_default__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__fct_orders
  join_to_view: workspace_default__orders
  join_type: always_left
  on_sql: ${workspace_default__fct_orders.order_id} =
    ${workspace_default__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__orders_interview
  join_to_view: workspace_default__orders
  join_type: always_left
  on_sql: ${workspace_default__orders_interview.order_id} =
    ${workspace_default__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__sales_data
  join_to_view: workspace_default__orders
  join_type: always_left
  on_sql: ${workspace_default__sales_data.order_id} =
    ${workspace_default__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__source_customer
  join_to_view: workspace_default__customer
  join_type: always_left
  on_sql: ${workspace_default__source_customer.customer_id} =
    ${workspace_default__customer.id}
  relationship_type: assumed_many_to_one

- join_from_view: workspace_default__stg_orders
  join_to_view: workspace_default__orders
  join_type: always_left
  on_sql: ${workspace_default__stg_orders.order_id} =
    ${workspace_default__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__cdc_orders
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__cdc_orders.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__source_customer
  join_to_view: omni_dbt__customer
  join_type: always_left
  on_sql: ${omni_dbt__source_customer.customer_id} = ${omni_dbt__customer.id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'orders' dbt semantic model (models/semantic_models.yml)
- join_from_view: omni_dbt__orders
  join_to_view: omni_dbt__customers
  join_type: always_left
  on_sql: ${omni_dbt__orders._entity_customer} =
    ${omni_dbt__customers._entity_customer}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__fact_orders
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__fact_orders.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__fct_orders
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__fct_orders.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__orders_interview
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__orders_interview.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__sales_data
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__sales_data.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_orders
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__stg_orders.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one
