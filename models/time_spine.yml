models:
  - name: time_spine
    description: "Daily time spine for MetricFlow semantic layer"

    time_spine:
      standard_granularity_column: date_day   # must match your SQL column name

    columns:
      - name: date_day
        granularity: day                       # must be 'day' or smaller