# Reference this view as workspace_default__my_first_dbt_model
catalog: workspace
schema: default
table_name: my_first_dbt_model

dimensions:
  id:
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
